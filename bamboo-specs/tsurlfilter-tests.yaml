---
version: 2
plan:
  project-key: AJL
  key: TSURLFILTERTEST
  name: tsurlfilter - tests

# All test jobs run in parallel in a single stage.
# Even though packages have dependencies on each other (e.g. tswebextension depends on tsurlfilter),
# each job runs its own `docker build --target test-X-output` and Docker BuildKit resolves the full
# dependency chain internally via FROM directives in the Dockerfile.
# Bamboo no longer needs to enforce build order — Docker handles it per job.
#
# Dependencies are described in the README.md file ("Dependency Tree" section).

stages:
  - Tests:
      manual: false
      final: false
      jobs:
        - Test Logger
        - Test CSSTokenizer
        - Test AGTree
        - Test TSUrlFilter
        - Test TSWebExtension
        - Test DNR-Rulesets
        - Test Examples - APIs and TSWebExtensions

Test Logger: !include 'jobs/tests/test-logger.yaml'
Test CSSTokenizer: !include 'jobs/tests/test-css-tokenizer.yaml'
Test AGTree: !include 'jobs/tests/test-agtree.yaml'
Test TSUrlFilter: !include 'jobs/tests/test-tsurlfilter.yaml'
Test TSWebExtension: !include 'jobs/tests/test-tswebextension.yaml'
Test DNR-Rulesets: !include 'jobs/tests/test-dnr-rulesets.yaml'
Test Examples - APIs and TSWebExtensions: !include 'jobs/tests/test-examples.yaml'

branches:
  create: for-pull-request
  delete:
    after-deleted-days: '1'
    after-inactive-days: '5'
  link-to-jira: true

labels: [ ]

other:
  concurrent-build-plugin: system-default
