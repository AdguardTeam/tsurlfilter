import {
    buildDiff,
    type BuildDiffParams,
    PATCH_EXTENSION,
} from './build';

const DiffBuilder = {
    buildDiff,
};

export {
    DiffBuilder,
    type BuildDiffParams,
    PATCH_EXTENSION,
};
