import { BrowserDetector } from '../../common/utils/browser-detector';

/**
 * Browser detector for MV3.
 */
// eslint-disable-next-line no-restricted-globals
export const browserDetectorMV3 = new BrowserDetector(self);
