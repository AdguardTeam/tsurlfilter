name: Publish release (monorepo package)

# Publishes a single monorepo package: parses packages/<package>/CHANGELOG.md,
# runs the Docker test/build targets for the package, publishes the .tgz
# (npm trusted publishing by default; `publish_target: ak` sends it to the
# internal Artifact Keeper npm registry instead — used by dnr-rulesets while
# npm throttles the shared GitHub egress IP, see publish-release.yml), and
# only then tags <package>-v<version> (after the publish succeeds), mirrors to
# the public repo, creates a GitHub Release, and notifies Slack.

on:
  workflow_call:
    inputs:
      package:
        description: 'Package directory name under packages/; also the tag prefix and .tgz base name (e.g. agtree)'
        type: string
        required: true
      npm_package_name:
        description: 'npm package name for Slack notification (e.g. @adguard/agtree)'
        type: string
        required: true
      target_repo:
        description: 'Public mirror repo (e.g. AdguardTeam/tsurlfilter)'
        type: string
        required: true
      ref:
        description: 'Ref to publish (merge commit SHA). Pin to the release PR merge commit; an empty ref checks out the current master tip.'
        type: string
        default: ''
      team:
        description: 'Team label for runner selection'
        type: string
        default: extensions
      environment:
        description: 'GitHub environment for npm publish protection rules'
        type: string
        default: npm
      slack_channel:
        description: 'Slack channel for release notifications'
        type: string
        default: '#adguard-extension-vcs'
      dry_run:
        description: 'Run npm publish with --dry-run (no registry contact). For CI testing.'
        type: boolean
        default: false
      publish_target:
        description: 'Registry to publish to: npm (default) or ak (internal Artifact Keeper npm registry)'
        type: string
        default: npm

jobs:
  parse:
    name: Parse changelog
    runs-on: team-${{ inputs.team }}
    timeout-minutes: 10
    permissions:
      contents: read
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
      version: ${{ steps.parse.outputs.version }}
      changelog: ${{ steps.parse.outputs.changelog }}
      prerelease: ${{ steps.parse.outputs.prerelease }}
      dist-tag: ${{ steps.meta.outputs.dist-tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref }}

      - name: Parse changelog
        id: parse
        uses: AdGuardSoftwareLimited/actions/actions/parse-changelog@master
        with:
          changelog-path: packages/${{ inputs.package }}/CHANGELOG.md

      - name: Compute tag and dist-tag
        id: meta
        shell: bash
        env:
          PACKAGE: ${{ inputs.package }}
          VERSION: ${{ steps.parse.outputs.version }}
          PRERELEASE_TYPE: ${{ steps.parse.outputs.prerelease-type }}
        run: |
          set -euo pipefail
          DIST_TAG="latest"
          if [ -n "$PRERELEASE_TYPE" ]; then
            DIST_TAG="$PRERELEASE_TYPE"
          fi
          echo "tag=${PACKAGE}-v${VERSION}" >> "$GITHUB_OUTPUT"
          echo "dist-tag=$DIST_TAG" >> "$GITHUB_OUTPUT"

  tag:
    needs: [parse, build, publish, publish-ak]
    # One of the publish legs is always skipped (the one not selected by
    # publish_target). Unless `if` contains one of the status-check functions
    # (success/failure/always/cancelled), GitHub applies the default
    # success() check, which is false whenever a needed job was skipped —
    # hence the plain `!inputs.dry_run` gate silently skipped tag/mirror/notify
    # on the first AK release (beta.1 got no tag, no mirror, no GitHub
    # Release, no Slack message). `!cancelled()` (the docs' recommended
    # alternative to always()) is the load-bearing term — dropping it
    # reinstates the default check and the skip. Requiring a successful
    # publish leg — not merely a non-failed one; same as
    # publish-stable-dnr-rulesets.yml — also keeps the tag away from runs
    # whose publish legs were both skipped (e.g. a failed build): such a tag
    # would block a re-release of the version (git-tag uses force: false).
    if: ${{ !cancelled() && !inputs.dry_run && (needs.publish.result == 'success' || needs.publish-ak.result == 'success') }}
    uses: AdGuardSoftwareLimited/actions/.github/workflows/git-tag.yml@master
    with:
      team: ${{ inputs.team }}
      tag: ${{ needs.parse.outputs.tag }}
      # force: false (the shared workflow default) so an existing tag is never
      # silently moved: re-releasing an already-released version must fail, not
      # rewrite the tag out from under the mirror. A fresh tag is created only
      # after the publish leg above (npm or Artifact Keeper) has succeeded.
      tag-message: ${{ needs.parse.outputs.tag }}
      ref: ${{ inputs.ref }}

  build:
    name: Test and build
    needs: parse
    runs-on: team-${{ inputs.team }}
    timeout-minutes: 30
    permissions:
      contents: read
      # No `actions: write` here: actions/upload-artifact@v4+ uploads with the
      # runner's ACTIONS_RUNTIME_TOKEN, not the repository GITHUB_TOKEN, so a
      # least-privilege `contents: read` is sufficient for this job.
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref }}

      - name: Inject package versions
        env:
          PACKAGE: ${{ inputs.package }}
          VERSION: ${{ needs.parse.outputs.version }}
        run: node scripts/inject-package-versions.mjs --package "$PACKAGE" --version "$VERSION"

      - name: Lint and test
        uses: ./.github/actions/docker-build
        with:
          target: test-${{ inputs.package }}-output
          dest: test-output

      - name: Check test exit code
        shell: bash
        run: |
          set -euo pipefail
          if [ ! -f test-output/exit-code.txt ]; then
            echo "::error::Test stage wrote no exit-code.txt (did the test target run?)"
            exit 1
          fi
          CODE=$(cat test-output/exit-code.txt)
          if [ "$CODE" != "0" ]; then
            echo "::error::Tests failed with exit code $CODE"
            exit "$CODE"
          fi

      - name: Build release artifacts
        uses: ./.github/actions/docker-build
        with:
          target: build-${{ inputs.package }}-output
          dest: build-output

      - name: Verify tarball contents
        shell: bash
        env:
          PACKAGE: ${{ inputs.package }}
          VERSION: ${{ needs.parse.outputs.version }}
        run: |
          set -euo pipefail
          bash scripts/ci/verify-tarball.sh "build-output/artifacts/${PACKAGE}.tgz" "$VERSION"

      - name: Verify dependency versions exist on npm
        # Fails the publish before npm even sees it if a `workspace:^`
        # dependency's floor version (its latest changelog heading) points at a
        # version that was never released — otherwise the packed tarball would
        # reference a nonexistent version and npm would resolve it wrongly.
        shell: bash
        env:
          PACKAGE: ${{ inputs.package }}
        run: |
          set -euo pipefail
          TGZ="build-output/artifacts/${PACKAGE}.tgz"

          # Extract the packed package.json once and let node read it from disk.
          # The script arrives via stdin (heredoc), so piping the tarball into
          # node too would be overridden by the heredoc and the script's stdin
          # would be empty — JSON.parse would throw on the first real release.
          tar -xOf "$TGZ" package/package.json > "$PACKAGE.package.json"
          PACKED_JSON_PATH="$PACKAGE.package.json" node - <<'NODE'
          const { execFileSync } = require('node:child_process');
          const fs = require('node:fs');

          // Read every @adguard/* dependency's floor version from the packed
          // manifest and check it exists on the registry.
          const pj = JSON.parse(fs.readFileSync(process.env.PACKED_JSON_PATH, 'utf8'));
          const deps = { ...pj.dependencies, ...pj.devDependencies };
          const floors = [];
          for (const [name, range] of Object.entries(deps)) {
            if (!name.startsWith('@adguard/') || name === pj.name) continue;
            const m = String(range).match(/^[\^~]?(\d+\.\d+\.\d+(?:-[a-z]+\.[0-9]+)?)$/);
            if (!m) {
              console.error(`::error::Unexpected dependency range ${name}@${range} in packed package.json`);
              process.exit(1);
            }
            floors.push([name, m[1]]);
          }
          const missing = [];
          for (const [name, version] of floors) {
            let out = '';
            try {
              out = execFileSync('npm', ['view', `${name}@${version}`, 'version', '--silent'], { encoding: 'utf8' }).trim();
            } catch { /* version not found */ }
            if (!out) missing.push(`${name}@${version}`);
          }
          if (missing.length > 0) {
            console.error(`::error::Dependency version${missing.length > 1 ? 's' : ''} not on npm: ${missing.join(', ')}`);
            console.error('::error::Release the dependency first, or fix the injected version.');
            process.exit(1);
          }
          console.log(`All ${floors.length} @adguard/* dependency floors exist on npm`);
          NODE

      - name: Upload npm package
        uses: actions/upload-artifact@v7
        with:
          name: npm-package-${{ inputs.package }}
          path: build-output/artifacts/${{ inputs.package }}.tgz
          # A missing tarball must fail the job, not pass green.
          if-no-files-found: error

  publish:
    needs: [parse, build]
    if: ${{ inputs.publish_target == 'npm' }}
    uses: AdGuardSoftwareLimited/actions/.github/workflows/deploy-to-npm.yml@master
    with:
      artifact-name: npm-package-${{ inputs.package }}
      package-file: ${{ inputs.package }}.tgz
      environment: ${{ inputs.environment }}
      tag: ${{ needs.parse.outputs.dist-tag }}
      dry-run: ${{ inputs.dry_run }}
    permissions:
      contents: read
      id-token: write

  # Publish to the internal Artifact Keeper npm registry instead of npm
  # (publish_target: ak). Currently used for dnr-rulesets while npm throttles
  # the shared GitHub egress IP (HTTP 429, AG-58865) — the same arrangement as
  # the stable line in publish-stable-dnr-rulesets.yml. No environment: the AK
  # publish is intentionally ungated (no approval), like the stable line —
  # only the npm path has the approval gate.
  publish-ak:
    name: Publish to Artifact Keeper
    needs: [parse, build]
    if: ${{ inputs.publish_target == 'ak' }}
    uses: AdGuardSoftwareLimited/actions/.github/workflows/deploy-to-ak-npm.yml@master
    with:
      team: ${{ inputs.team }}
      artifact-name: npm-package-${{ inputs.package }}
      tag: ${{ needs.parse.outputs.dist-tag }}
      environment: ''
      dry-run: ${{ inputs.dry_run }}
    # deploy-to-ak-npm.yml reads `secrets.ARTIFACT_KEEPER_API_KEY` but does not
    # declare it in `on.workflow_call.secrets` — GitHub fails any call that
    # passes a secret the called workflow does not declare. `secrets: inherit`
    # is required (the pattern used by every AK consumer, including
    # publish-stable-dnr-rulesets.yml); ARTIFACT_KEEPER_URL is an org variable
    # and does not need forwarding.
    secrets: inherit
    permissions:
      contents: read

  mirror-and-release:
    needs: [parse, tag, publish, publish-ak]
    # Same skip-propagation fix as `tag` — see the full explanation there.
    # `!cancelled()` must stay: without a status-check function the default
    # success() check skips this job over the always-skipped publish leg. The
    # tag must also have succeeded — a failed tag (e.g. the tag already
    # exists) must not be mirrored or given a GitHub Release, and a version
    # whose publish legs were both skipped (e.g. a failed build) must not be
    # released either.
    if: ${{ !cancelled() && !inputs.dry_run && needs.tag.result == 'success' && (needs.publish.result == 'success' || needs.publish-ak.result == 'success') }}
    uses: AdGuardSoftwareLimited/actions/.github/workflows/mirror-and-release.yml@master
    with:
      team: ${{ inputs.team }}
      target_repo: ${{ inputs.target_repo }}
      tag: ${{ needs.parse.outputs.tag }}
      changelog: ${{ needs.parse.outputs.changelog }}
      artifact_name: npm-package-${{ inputs.package }}
      # parse-changelog emits strings; the `== 'true'` comparison coerces to boolean
      # for the boolean workflow_call input of mirror-and-release.yml.
      prerelease: ${{ needs.parse.outputs.prerelease == 'true' }}
    secrets: inherit
    permissions:
      actions: write
      contents: write
      id-token: write

  notify:
    name: Notify
    needs: [parse, mirror-and-release]
    # Same skip-propagation fix as `tag` and `mirror-and-release` — see the
    # full explanation there. `!cancelled()` must stay: without a status-check
    # function the default success() check skips this job whenever any needed
    # job upstream was skipped, and one publish leg is always skipped (the one
    # not selected by publish_target) — so without it `notify` never runs on a
    # successful release.
    if: ${{ !cancelled() && needs.mirror-and-release.result == 'success' }}
    runs-on: team-${{ inputs.team }}
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Send Slack notification (npm)
        if: ${{ inputs.publish_target == 'npm' }}
        uses: AdGuardSoftwareLimited/actions/actions/slack@master
        with:
          channel: ${{ inputs.slack_channel }}
          message: ':white_check_mark: *${{ inputs.npm_package_name }}* *v${{ needs.parse.outputs.version }}* published to npm — <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|view run>'
          color: good

      # Artifact Keeper publishes are consumed from the internal AK registry
      # (the browser-extension auto-build pulls from there when npm throttles),
      # so the success message carries the install instructions. The registry
      # redirect is project-scoped (`--location=project`) with an explicit
      # cleanup step: a global `@adguard:registry` in ~/.npmrc would redirect
      # every other @adguard install to the internal host, which is
      # unreachable off the VPN.
      - name: Send Slack notification (Artifact Keeper)
        if: ${{ inputs.publish_target == 'ak' }}
        uses: AdGuardSoftwareLimited/actions/actions/slack@master
        with:
          channel: ${{ inputs.slack_channel }}
          message: |-
            :white_check_mark: *${{ inputs.npm_package_name }}* *v${{ needs.parse.outputs.version }}*
            published to Artifact Keeper (dist-tag: `${{ needs.parse.outputs.dist-tag }}`) — <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|view run>

            To use it, install from the internal AK registry (VPN only). Scope the registry to the project — a global `@adguard:registry` would break every other @adguard install off the VPN:
            `npm config set @adguard:registry ${{ vars.ARTIFACT_KEEPER_URL }}/npm/npm-internal --location=project`
            `pnpm add ${{ inputs.npm_package_name }}@${{ needs.parse.outputs.version }}`
            Cleanup when done: `npm config delete @adguard:registry --location=project`
          color: good

  notify-failure:
    name: Notify on failure
    needs: [parse, build, publish, publish-ak, tag, mirror-and-release, notify]
    # Once the release PR merges the publish is unattended, so a failed release
    # (npm 409, OIDC hiccup, mirror fault) must reach the channel — the old
    # release.yml posted to Slack with always(). A cancelled run is not a
    # failure.
    if: ${{ failure() }}
    runs-on: team-${{ inputs.team }}
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Send Slack notification
        uses: AdGuardSoftwareLimited/actions/actions/slack@master
        with:
          channel: ${{ inputs.slack_channel }}
          message: ':x: *${{ inputs.npm_package_name }}* publish failed — <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|view run>'
          color: danger
